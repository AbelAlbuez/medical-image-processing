# P2a Hard Shape Proxy

Applies train-fold learned binary component filters using normalized shape
proxies.

Verdict: hard rejection is too brittle; it reduces some false positives but does
not create a robust usable operating point.

